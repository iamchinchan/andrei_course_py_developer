# Selenium Automation Project

A repository folder for practicing and building browser automation scripts using Selenium in Python.

## Structure
- `tests/`: Test scripts and automation scripts
- `drivers/`: WebDriver binaries (if needed)
- `requirements.txt`: Python dependencies
